name: Position Monitor
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Check Position
        run: curl -s https://poolforge-v2.vercel.app/api/monitor-position