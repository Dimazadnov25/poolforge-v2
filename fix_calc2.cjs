const fs=require("fs");let c=fs.readFileSync("src/components/RaydiumDashboard.jsx","utf8");c=c.replace("if(ps&&ps.price)setUsdc((parseFloat(e.target.value||0)*parseFloat(ps.price)).toFixed(2))","if(ps&&ps.price){const p=parseFloat(ps.price);if(p>0)setUsdc((parseFloat(e.target.value||0)*p).toFixed(2))}");c=c.replace("if(ps&&ps.price)setSol((parseFloat(e.target.value||0)/parseFloat(ps.price)).toFixed(4))","if(ps&&ps.price){const p=parseFloat(ps.price);if(p>0)setSol((parseFloat(e.target.value||0)/p).toFixed(6))}");fs.writeFileSync("src/components/RaydiumDashboard.jsx",c);console.log("ok:",c.includes("if(p>0)setUsdc"));
